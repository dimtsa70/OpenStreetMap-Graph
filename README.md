This project is about creating a graph using real data from OpenStreetMap. In general, it adds/deletes nodes, calculates shortest path from route A->B using Dijkstra's algorithm,
it compacts the graph depending on the direction of each road(one/double way) and permeation of the graph with bfs and dfs algorithms.
